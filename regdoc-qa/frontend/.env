# Leave empty for local dev — the Vite dev server proxies /api to http://localhost:8000.
# For production, set this to your deployed backend origin (no trailing slash), e.g.:
# VITE_API_BASE_URL=https://regdoc-qa-backend.up.railway.app
VITE_API_BASE_URL=
